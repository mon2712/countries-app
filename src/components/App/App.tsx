import React from 'react';

import Routes from 'routes';

const App = (): React.ReactElement => {
    return (
        <div className="App">
            <Routes />
        </div>
    );
};

export default App;
